$(function () {
  $('.bike__slider, .slider__items').slick({
    arrows: false,
    dots: true,
    // style for animation 
    fade: true,
    speed: 500,
    // autoplay for animation
    // autoplay: true,
    // autoplaySpeed: 1000,
  });
});
